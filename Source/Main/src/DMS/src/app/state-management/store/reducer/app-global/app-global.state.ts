import { StructureTreeSettingsStateModel } from '../../models/app-global/state/structure-tree-settings.state.model';

export interface AppGlobalState {
    structureTreeSettings: StructureTreeSettingsStateModel;
}